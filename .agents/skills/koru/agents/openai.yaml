interface:
  display_name: "Koru"
  short_description: "Repo-specific patterns and workflows for koru"
  default_prompt: "Use the koru repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true